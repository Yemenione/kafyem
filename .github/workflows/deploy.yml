# Deployment is handled directly by Hostinger
# This workflow is disabled
name: Disabled Workflow

on:
  push:
    branches: [ never ]

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is disabled. Deployment is handled by Hostinger."
